{
  "name": "portfolio",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "dev:desktop": "vite --config vite.config.desktop.js",
    "build:portfolio": "vite build",
    "build:desktop": "vite build --config vite.config.desktop.js",
    "build": "npm run build:portfolio && npm run build:desktop",
    "preview": "vite preview"
  },
  "dependencies": {
    "gsap": "^3.12.5"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
